import { Camera } from 'lucide-react'
import EmptyState from '../../components/EmptyState'

export default function AddPhotoTab() {
  return <EmptyState icon={Camera} title="Photo logging" subtitle="Coming in a later phase" />
}
