import { Search } from 'lucide-react'
import EmptyState from '../../components/EmptyState'

export default function AddSearchTab() {
  return <EmptyState icon={Search} title="Food search" subtitle="Coming in a later phase" />
}
