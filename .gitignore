node_modules/
dist/
*.tsbuildinfo
vite.config.js
vite.config.d.ts
.frugal-fable/
*.local

# Garmin auto-sync secrets / output — never commit these
serviceAccount.json
scripts/serviceAccount.json
rung-import.json
scripts/garmin-sync.log

# Garmin link keypair — the private half must never be committed
garmin-link-private.key
*.pkcs8

# Python bytecode
__pycache__/
*.pyc
